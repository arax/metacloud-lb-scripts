{{START}} I'm a Syslog Notifier for OpenNebula. There is something going on with [<%= vm_template.NAME %>] owned by [<%= user_identity %>], hook <%= vm_state.to_s.upcase %> has been triggered. This might be useful to you: VMID -> <%= vm_template.ID %> || USERNAME -> <%= vm_template.UNAME %> || USERGROUP -> <%= vm_template.GNAME %> || VMNAME -> <%= vm_template.NAME %> || VMSTATE -> <%= vm_template.STATE %> || VMLCMSTATE -> <%= vm_template.LCM_STATE %> || VMSTARTTIME -> <%= vm_template.STIME %> || VMENDTIME -> <%= vm_template.ETIME %> || VMDEPLOYID -> <%= vm_template.DEPLOY_ID %> || VMMEMORY -> <%= vm_template.MEMORY %> || VMCPU -> <%= vm_template.CPU %> || VMNETTX -> <%= vm_template.NET_TX %> || VMNETRX -> <%= vm_template.NET_RX %> || VMHOSTNAME -> <%= vm_template.HOSTNAME %> || VMIMAGES -> <% vm_template.IMAGES.each do |image| %><%= image %>, <% end %> || VMIPS -> <% vm_template.IPS.each do |ip| %><%= ip %>, <% end %> || VMHOSTS -> <% vm_template.HOSTS.each do |host| %><%= host %>, <% end %>. This VM was launched with the following requirements: CPU Cores -> <%= vm_template.REQ_VCPU %> || MEMORY -> <%= vm_template.REQ_MEMORY %>. Usage record says: <%= vm_usage %>. KRB5 ticket for this machine should look like "host/<%= vm_template.HOSTNAME || 'UNKNOWN' %>@<%= krb_host_realm %>" {{END}}
